[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "cascade"
version = "0.1.0"
description = "HEP Workflow Orchestrator"
dependencies = [
    "litellm>=1.0.0",
    "python-dotenv>=1.0.0"
]
